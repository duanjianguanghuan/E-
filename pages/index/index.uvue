<template>
	<view :class="['home-page', themeClass]">
		<!-- 状态栏占位 -->
		<view :class="['status-bar', themeClass]"></view>

		<!-- 顶部导航 -->
		<view :class="['navbar', themeClass]">
			<view class="navbar-left">
				<text :class="['greeting', themeClass]">E游周享</text>
				<text :class="['subtitle', themeClass]">Epic 每周免费游戏资讯</text>
			</view>
			<view :class="['nav-actions', themeClass]">
				<view :class="['icon-btn', themeClass]" @tap="refreshData">
					<text :class="['icon-text', themeClass]">↻</text>
				</view>
				<view :class="['icon-btn', themeClass]" @tap="goSettings">
					<text :class="['icon-text', themeClass]">⚙</text>
				</view>
			</view>
		</view>

		<!-- 加载状态 -->
		<view v-if="loading" :class="['loading-wrap', themeClass]">
			<text :class="['loading-text', themeClass]">加载中...</text>
		</view>

		<!-- 错误状态 -->
		<view v-if="errorMsg && !loading" :class="['error-wrap', themeClass]">
			<text class="error-emoji">😵</text>
			<text :class="['error-text', themeClass]">{{ errorMsg }}</text>
			<view :class="['retry-btn', themeClass]" @tap="refreshData">
				<text :class="['retry-text', themeClass]">点击重试</text>
			</view>
		</view>

		<!-- 数据内容 -->
		<view v-if="!loading && !errorMsg" class="content-wrap">
			<!-- 本周免费游戏 -->
			<view class="section-header">
				<text :class="['section-label', themeClass]">🎮 本周免费</text>
				<text :class="['section-badge', themeClass]">限时领取</text>
			</view>

			<scroll-view scroll-x="true" class="card-scroll" show-scrollbar="false">
				<view
					:class="['game-card', themeClass]"
					v-for="game in freeNowGames"
					:key="game.id"
					@tap="() => openGameLink(game.link)"
				>
					<image
						:class="['game-cover', themeClass]"
						:src="game.cover"
						mode="aspectFill"
					/>
					<view class="game-info">
						<text :class="['game-title', themeClass]">{{ game.title }}</text>
						<text :class="['game-desc', themeClass]">{{ game.description }}</text>
						<view :class="['game-meta', themeClass]">
							<text :class="['game-tag-free', themeClass]">免费</text>
							<text :class="['game-time', themeClass]">
								截止 {{ formatFreeTime(game.free_end) }}
							</text>
						</view>
					</view>
				</view>
			</scroll-view>

			<!-- 即将推出 -->
			<view class="section-header">
				<text :class="['section-label', themeClass]">📅 即将推出</text>
				<text :class="['section-badge-outline', themeClass]">敬请期待</text>
			</view>

			<view :class="['list-item', themeClass]" v-for="game in upcomingGames" :key="game.id">
				<image
					:class="['list-cover', themeClass]"
					:src="game.cover"
					mode="aspectFill"
				/>
				<view class="list-content">
					<text :class="['list-title', themeClass]">{{ game.title }}</text>
					<text :class="['list-desc', themeClass]">{{ game.description }}</text>
					<view class="list-meta">
						<text :class="['meta-text', themeClass]">
							{{ formatFreeTime(game.free_start) }} 起
						</text>
					</view>
				</view>
				<view :class="['list-badge', themeClass]">
					<text :class="['badge-text', themeClass]">预告</text>
				</view>
			</view>

			<!-- 底部提示 -->
			<view :class="['footer-tip', themeClass]">
				<text :class="['tip-text', themeClass]">
					数据来源: 60s.viki.moe · 点击卡片前往 Epic 商店领取
				</text>
			</view>
		</view>

		<!-- 底部安全区 -->
		<view class="bottom-safe"></view>
	</view>
</template>

<script setup lang="uts">
	import { useTheme } from '@/composables/useTheme'
	import { fetchEpicGames, getFreeNowGames, getUpcomingGames, formatFreeTime } from '@/api/epic'
	import type { EpicGame } from '@/api/epic'
	import { ref, computed } from 'vue'

	const { currentTheme } = useTheme()

	const themeClass = computed(() => {
		return currentTheme.value === 'liquid-glass' ? 'theme-glass' : 'theme-normal'
	})

	const loading = ref(false)
	const errorMsg = ref<string | null>(null)
	const freeNowGames = ref<EpicGame[]>([])
	const upcomingGames = ref<EpicGame[]>([])

	async function loadGames() {
		loading.value = true
		errorMsg.value = null

		const result = await fetchEpicGames()
		if (result.success && result.data) {
			freeNowGames.value = getFreeNowGames(result.data.data)
			upcomingGames.value = getUpcomingGames(result.data.data)
		} else {
			errorMsg.value = result.message ?? '加载失败'
		}
		loading.value = false
	}

	function refreshData() {
		loadGames()
	}

	function openGameLink(link: string) {
		if (!link || link.includes('[]')) {
			uni.showToast({
				title: '暂未公布，敬请期待',
				icon: 'none',
			})
			return
		}
		uni.showToast({
			title: '即将跳转至 Epic 商店',
			icon: 'none',
		})
	}

	function goSettings() {
		uni.navigateTo({
			url: '/pages/settings/settings',
		})
	}

	// 初始化加载
	loadGames()
</script>

<style>
	/* ========== 普通主题 ========== */
	.home-page {
		min-height: 100vh;
	}

	.home-page.theme-normal {
		background-color: #f2f2f7;
	}

	.status-bar {
		height: 44px;
		background-color: #ffffff;
	}

	.navbar {
		flex-direction: row;
		justify-content: space-between;
		align-items: center;
		padding: 16px 20px 20px 20px;
		background-color: #ffffff;
	}

	.navbar-left {
		flex-direction: column;
	}

	.greeting {
		font-size: 24px;
		font-weight: 800;
		color: #1c1c1e;
		letter-spacing: -0.5px;
	}

	.subtitle {
		font-size: 14px;
		color: #8e8e93;
		margin-top: 2px;
	}

	.nav-actions {
		flex-direction: row;
		gap: 10px;
	}

	.icon-btn {
		width: 42px;
		height: 42px;
		border-radius: 14px;
		background-color: #f2f2f7;
		align-items: center;
		justify-content: center;
	}

	.icon-text {
		font-size: 20px;
	}

	/* ========== 加载 / 错误 ========== */
	.loading-wrap {
		padding: 80px 20px;
		align-items: center;
		justify-content: center;
	}

	.loading-text {
		font-size: 15px;
		color: #8e8e93;
	}

	.error-wrap {
		padding: 80px 40px;
		align-items: center;
		gap: 12px;
	}

	.error-emoji {
		font-size: 48px;
	}

	.error-text {
		font-size: 14px;
		color: #8e8e93;
		text-align: center;
		line-height: 1.5;
	}

	.retry-btn {
		margin-top: 8px;
		padding: 10px 28px;
		border-radius: 12px;
		background-color: #007aff;
	}

	.retry-text {
		font-size: 14px;
		color: #ffffff;
		font-weight: 600;
	}

	/* ========== 区域标题 ========== */
	.section-header {
		flex-direction: row;
		justify-content: space-between;
		align-items: center;
		padding: 20px 20px 12px 20px;
	}

	.section-label {
		font-size: 17px;
		font-weight: 700;
		color: #1c1c1e;
	}

	.section-badge {
		font-size: 11px;
		color: #ffffff;
		background-color: #ff3b30;
		padding: 3px 10px;
		border-radius: 6px;
		font-weight: 600;
	}

	.section-badge-outline {
		font-size: 11px;
		color: #007aff;
		background-color: #e8f2ff;
		padding: 3px 10px;
		border-radius: 6px;
		font-weight: 600;
	}

	.content-wrap {
		padding-bottom: 20px;
	}

	/* ========== 游戏卡片（横向滚动）========== */
	.card-scroll {
		width: 100%;
		white-space: nowrap;
		padding-left: 20px;
	}

	.game-card {
		width: 240px;
		display: inline-flex;
		flex-direction: column;
		margin-right: 12px;
		border-radius: 18px;
		background-color: #ffffff;
		box-shadow: 0 1px 4px rgba(0, 0, 0, 0.04);
		border: 1px solid rgba(0, 0, 0, 0.06);
		overflow: hidden;
		vertical-align: top;
	}

	.game-cover {
		width: 100%;
		height: 135px;
		background-color: #e8e8ed;
	}

	.game-info {
		padding: 12px 14px 14px 14px;
		flex-direction: column;
	}

	.game-title {
		font-size: 15px;
		font-weight: 700;
		color: #1c1c1e;
		lines: 1;
	}

	.game-desc {
		font-size: 12px;
		color: #8e8e93;
		margin-top: 4px;
		lines: 1;
	}

	.game-meta {
		flex-direction: row;
		align-items: center;
		gap: 8px;
		margin-top: 10px;
	}

	.game-tag-free {
		font-size: 11px;
		color: #ffffff;
		background-color: #34c759;
		padding: 2px 8px;
		border-radius: 6px;
		font-weight: 600;
	}

	.game-time {
		font-size: 11px;
		color: #b0b0b8;
	}

	/* ========== 即将推出列表 ========== */
	.list-item {
		flex-direction: row;
		align-items: center;
		padding: 12px 20px;
		margin: 0 16px 10px 16px;
		border-radius: 16px;
		background-color: #ffffff;
		box-shadow: 0 1px 4px rgba(0, 0, 0, 0.04);
		border: 1px solid rgba(0, 0, 0, 0.06);
	}

	.list-cover {
		width: 64px;
		height: 40px;
		border-radius: 6px;
		background-color: #e8e8ed;
		margin-right: 14px;
		flex-shrink: 0;
	}

	.list-content {
		flex: 1;
		flex-direction: column;
	}

	.list-title {
		font-size: 15px;
		font-weight: 700;
		color: #1c1c1e;
		lines: 1;
	}

	.list-desc {
		font-size: 12px;
		color: #8e8e93;
		margin-top: 3px;
		lines: 1;
	}

	.list-meta {
		flex-direction: row;
		gap: 8px;
		margin-top: 4px;
	}

	.meta-text {
		font-size: 11px;
		color: #b0b0b8;
	}

	.list-badge {
		padding: 3px 10px;
		border-radius: 6px;
		background-color: #f2f2f7;
		margin-left: 8px;
		flex-shrink: 0;
	}

	.badge-text {
		font-size: 11px;
		color: #8e8e93;
		font-weight: 500;
	}

	/* ========== 底部提示 ========== */
	.footer-tip {
		padding: 20px;
		align-items: center;
	}

	.tip-text {
		font-size: 11px;
		color: #c7c7cc;
		text-align: center;
	}

	.bottom-safe {
		height: 34px;
	}

	/* ===================================== */
	/*          🧊 液体玻璃主题样式           */
	/* ===================================== */

	.home-page.theme-glass {
		background: linear-gradient(160deg, #0a0a1a 0%, #1a1030 25%, #0d1b2a 50%, #0f0f24 75%, #0a0a1a 100%);
		background-attachment: fixed;
	}

	.status-bar.theme-glass {
		background: rgba(255, 255, 255, 0.03);
		backdrop-filter: blur(20px);
		-webkit-backdrop-filter: blur(20px);
	}

	.navbar.theme-glass {
		background: rgba(255, 255, 255, 0.03);
		backdrop-filter: blur(20px);
		-webkit-backdrop-filter: blur(20px);
		border-bottom: 1px solid rgba(255, 255, 255, 0.05);
	}

	.greeting.theme-glass {
		color: rgba(255, 255, 255, 0.92);
	}

	.subtitle.theme-glass {
		color: rgba(190, 205, 235, 0.55);
	}

	.icon-btn.theme-glass {
		background: rgba(255, 255, 255, 0.06);
		backdrop-filter: blur(12px);
		-webkit-backdrop-filter: blur(12px);
		border: 1px solid rgba(255, 255, 255, 0.08);
		border-radius: 14px;
	}

	.loading-text.theme-glass {
		color: rgba(200, 210, 240, 0.5);
	}

	.error-text.theme-glass {
		color: rgba(200, 210, 240, 0.5);
	}

	.retry-btn.theme-glass {
		background: rgba(120, 160, 255, 0.25);
		backdrop-filter: blur(8px);
		-webkit-backdrop-filter: blur(8px);
		border: 1px solid rgba(120, 160, 255, 0.2);
	}

	.retry-text.theme-glass {
		color: rgba(180, 210, 255, 0.9);
	}

	.section-label.theme-glass {
		color: rgba(240, 245, 255, 0.88);
	}

	.section-badge-outline.theme-glass {
		background: rgba(120, 160, 255, 0.15);
		color: rgba(140, 180, 255, 0.85);
	}

	.game-card.theme-glass {
		background: rgba(255, 255, 255, 0.04);
		backdrop-filter: blur(16px);
		-webkit-backdrop-filter: blur(16px);
		border: 1px solid rgba(255, 255, 255, 0.07);
		border-radius: 20px;
		box-shadow:
			0 8px 24px rgba(0, 0, 0, 0.3),
			inset 0 1px 0 rgba(255, 255, 255, 0.05);
	}

	.game-cover.theme-glass {
		background-color: rgba(255, 255, 255, 0.03);
	}

	.game-title.theme-glass {
		color: rgba(235, 240, 255, 0.9);
	}

	.game-desc.theme-glass {
		color: rgba(180, 195, 225, 0.5);
	}

	.game-meta.theme-glass {
		gap: 8px;
	}

	.game-tag-free.theme-glass {
		background: rgba(52, 199, 89, 0.3);
		color: rgba(130, 255, 160, 0.9);
	}

	.game-time.theme-glass {
		color: rgba(160, 175, 210, 0.4);
	}

	.list-item.theme-glass {
		background: rgba(255, 255, 255, 0.04);
		backdrop-filter: blur(14px);
		-webkit-backdrop-filter: blur(14px);
		border: 1px solid rgba(255, 255, 255, 0.06);
		border-radius: 16px;
		box-shadow:
			0 4px 16px rgba(0, 0, 0, 0.25),
			inset 0 1px 0 rgba(255, 255, 255, 0.04);
	}

	.list-cover.theme-glass {
		background-color: rgba(255, 255, 255, 0.03);
	}

	.list-title.theme-glass {
		color: rgba(235, 240, 255, 0.9);
	}

	.list-desc.theme-glass {
		color: rgba(180, 195, 225, 0.5);
	}

	.meta-text.theme-glass {
		color: rgba(160, 175, 210, 0.4);
	}

	.list-badge.theme-glass {
		background: rgba(255, 255, 255, 0.06);
	}

	.badge-text.theme-glass {
		color: rgba(180, 200, 230, 0.5);
	}

	.tip-text.theme-glass {
		color: rgba(160, 180, 210, 0.3);
	}
</style>