<template>
	<view :class="['home-page', themeClass]">
		<!-- 顶部导航栏 -->
		<view :class="['navbar-wrap', themeClass]">
			<view :class="['status-bar', themeClass]"></view>
			<view :class="['navbar', themeClass]">
				<view class="navbar-left">
					<text :class="['greeting', themeClass]">E游周享</text>
					<text :class="['subtitle', themeClass]">Epic 每周免费游戏</text>
				</view>
				<view class="navbar-right">
					<view :class="['icon-btn', themeClass, { loading: isRefreshing }]" @tap="onRefreshTap">
						<text :class="['icon-text', themeClass]">{{ isRefreshing ? '⟳' : '↻' }}</text>
					</view>
					<view :class="['icon-btn', themeClass]" @tap="goSettings">
						<text :class="['icon-text', themeClass]">⚙</text>
					</view>
				</view>
			</view>
		</view>

		<!-- 页面内容区 -->
		<scroll-view
			scroll-y="true"
			class="content-scroll"
			:refresher-enabled="true"
			:refresher-triggered="isRefreshing"
			@refresherrefresh="onPullRefresh"
			@scrolltolower="onScrollBottom"
		>
			<!-- 骨架屏加载态 -->
			<view v-if="isSkeleton" class="skeleton-wrap">
				<view class="skeleton-section">
					<view class="skeleton-header"></view>
					<scroll-view scroll-x="true" class="skeleton-scroll">
						<view class="skeleton-card-list">
							<view class="skeleton-card" v-for="i in 3" :key="i">
								<view class="skeleton-img"></view>
								<view class="skeleton-text"></view>
								<view class="skeleton-text short"></view>
							</view>
						</view>
					</scroll-view>
				</view>
				<view class="skeleton-section">
					<view class="skeleton-header"></view>
					<view class="skeleton-list">
						<view class="skeleton-list-item" v-for="i in 4" :key="i">
							<view class="skeleton-thumb"></view>
							<view class="skeleton-content">
								<view class="skeleton-text"></view>
								<view class="skeleton-text short"></view>
							</view>
						</view>
					</view>
				</view>
			</view>

			<!-- 错误态 -->
			<view v-else-if="hasError" :class="['state-wrap', themeClass]">
				<text class="state-emoji">😵</text>
				<text :class="['state-text', themeClass]">{{ errorMsg }}</text>
				<view :class="['state-btn', themeClass]" @tap="loadGames">
					<text :class="['state-btn-text', themeClass]">重新加载</text>
				</view>
			</view>

			<!-- 空数据态 -->
			<view v-else-if="isEmpty" :class="['state-wrap', themeClass]">
				<text class="state-emoji">📭</text>
				<text :class="['state-text', themeClass]">本周暂无免费游戏</text>
				<text :class="['state-sub', themeClass]">下周再来看看吧</text>
			</view>

			<!-- 数据内容 -->
			<view v-else class="data-wrap">
				<!-- 本周免费 -->
				<view v-if="freeNowGames.length > 0">
					<view class="section-header">
						<view class="section-title-row">
							<text :class="['section-label', themeClass]">本周免费</text>
							<text :class="['section-badge', themeClass]">限时</text>
						</view>
					</view>

					<scroll-view scroll-x="true" class="card-scroll" enhanced="true" bounces="false">
						<view class="card-list">
							<view
								:class="['game-card', themeClass]"
								v-for="game in freeNowGames"
								:key="game.id"
								@tap="openGameLink(game)"
							>
								<view class="game-cover-wrap">
									<image
										:class="['game-cover', themeClass]"
										:src="game.cover"
										mode="aspectFill"
										lazy-load="true"
										@error="onImageError"
									/>
									<view :class="['free-tag', themeClass]">免费</view>
								</view>
								<view class="game-body">
									<text :class="['game-title', themeClass]">{{ game.title }}</text>
									<text :class="['game-desc', themeClass]">{{ game.seller }}</text>
									<view class="game-footer">
										<text :class="['game-time', themeClass]">
											截止 {{ formatDate(game.free_end) }}
										</text>
									</view>
								</view>
							</view>
						</view>
					</scroll-view>
				</view>

				<!-- 即将推出 -->
				<view v-if="upcomingGames.length > 0">
					<view class="section-header">
						<view class="section-title-row">
							<text :class="['section-label', themeClass]">下周预告</text>
							<text :class="['section-badge-outline', themeClass]">敬请期待</text>
						</view>
					</view>

					<view class="upcoming-list">
						<view
							:class="['upcoming-item', themeClass]"
							v-for="game in upcomingGames"
							:key="game.id"
							@tap="openGameLink(game)"
						>
							<image
								:class="['upcoming-thumb', themeClass]"
								:src="game.cover"
								mode="aspectFill"
								lazy-load="true"
								@error="onImageError"
							/>
							<view class="upcoming-content">
								<text :class="['upcoming-title', themeClass]">{{ game.title }}</text>
								<text :class="['upcoming-seller', themeClass]">{{ game.seller }}</text>
								<text :class="['upcoming-date', themeClass]">
									{{ formatDate(game.free_start) }} 起免费
								</text>
							</view>
							<text :class="['upcoming-arrow', themeClass]">›</text>
						</view>
					</view>
				</view>

				<!-- 底部提示 -->
				<view :class="['footer-tip', themeClass]">
					<text :class="['tip-text', themeClass]">数据来源: 60s.viki.moe</text>
				</view>
			</view>

			<!-- 底部安全区 -->
			<view class="bottom-safe"></view>
		</scroll-view>
	</view>
</template>

<script setup lang="uts">
	import { useTheme } from '@/composables/useTheme'
	import { fetchEpicGames, getFreeNowGames, getUpcomingGames } from '@/api/epic'
	import type { EpicGame } from '@/api/epic'
	import { ref, computed } from 'vue'

	const { currentTheme, themeClass } = useTheme()

	/* ========== 状态 ========== */
	const isSkeleton = ref(true)
	const isRefreshing = ref(false)
	const errorMsg = ref<string | null>(null)
	const freeNowGames = ref<EpicGame[]>([])
	const upcomingGames = ref<EpicGame[]>([])

	const hasError = computed(() => errorMsg.value !== null)
	const isEmpty = computed(() => !isSkeleton.value && !hasError.value && freeNowGames.value.length === 0 && upcomingGames.value.length === 0)

	/* ========== 数据加载 ========== */
	async function loadGames(forceRefresh: boolean = false) {
		errorMsg.value = null

		const result = await fetchEpicGames(forceRefresh)
		if (result.success && result.data) {
			freeNowGames.value = getFreeNowGames(result.data.data)
			upcomingGames.value = getUpcomingGames(result.data.data)
		} else {
			errorMsg.value = result.message
		}
		isSkeleton.value = false
		isRefreshing.value = false
	}

	/* ========== 下拉刷新 ========== */
	function onPullRefresh() {
		if (isRefreshing.value) return
		isRefreshing.value = true
		loadGames(true)
	}

	/* ========== 刷新按钮点击（防抖） ========== */
	let refreshTimer: number | null = null
	function onRefreshTap() {
		if (isRefreshing.value) return
		if (refreshTimer !== null) {
			clearTimeout(refreshTimer)
			refreshTimer = null
			return
		}
		isRefreshing.value = true
		loadGames(true)
		refreshTimer = setTimeout(() => {
			refreshTimer = null
		}, 1000)
	}

	/* ========== 滚动到底部 ========== */
	function onScrollBottom() {
		// 预留扩展：加载更多
	}

	/* ========== 图片加载失败 ========== */
	function onImageError(e: any) {
		console.log('Image load error:', e)
	}

	/* ========== 打开游戏链接 ========== */
	function openGameLink(game: EpicGame) {
		if (!game.link || game.link.includes('[]')) {
			uni.showToast({
				title: '游戏链接暂未公布',
				icon: 'none',
				duration: 2000,
			})
			return
		}
		uni.showToast({
			title: '正在打开 Epic 商店',
			icon: 'loading',
			duration: 1500,
		})
		setTimeout(() => {
			// #ifdef H5
			window.open(game.link, '_blank')
			// #endif
			// #ifndef H5
			uni.setClipboardData({
				data: game.link,
				showToast: true,
				toastTitle: '链接已复制，请在浏览器中打开',
			})
			// #endif
		}, 800)
	}

	/* ========== 跳转设置 ========== */
	function goSettings() {
		uni.navigateTo({
			url: '/pages/settings/settings',
		})
	}

	/* ========== 日期格式化 ========== */
	function formatDate(dateStr: string): string {
		if (!dateStr) return ''
		const parts = dateStr.split(' ')
		if (parts.length > 0) {
			return parts[0].replace(/\//g, '-')
		}
		return dateStr
	}

	/* ========== 生命周期 ========== */
	onMounted(() => {
		loadGames()
	})

	onUnmounted(() => {
		if (refreshTimer !== null) {
			clearTimeout(refreshTimer)
			refreshTimer = null
		}
	})
</script>

<style>
	/* ===================================== */
	/*              通用样式                  */
	/* ===================================== */

	.home-page {
		height: 100vh;
		flex-direction: column;
	}

	.navbar-wrap {
		flex-shrink: 0;
	}

	.status-bar {
		height: 44px;
	}

	.navbar {
		flex-direction: row;
		justify-content: space-between;
		align-items: center;
		padding: 12px 20px 16px 20px;
	}

	.navbar-left {
		flex-direction: column;
	}

	.navbar-right {
		flex-direction: row;
		align-items: center;
	}

	.greeting {
		font-size: 22px;
		font-weight: 800;
	}

	.subtitle {
		font-size: 13px;
		margin-top: 2px;
	}

	.icon-btn {
		width: 40px;
		height: 40px;
		border-radius: 12px;
		align-items: center;
		justify-content: center;
		margin-left: 8px;
	}

	.icon-text {
		font-size: 18px;
	}

	.content-scroll {
		flex: 1;
	}

	.skeleton-wrap {
		padding: 0 4px;
	}

	.skeleton-section {
		margin-top: 16px;
	}

	.skeleton-header {
		height: 20px;
		width: 100px;
		margin: 20px 20px 12px 20px;
	}

	.skeleton-scroll {
		width: 100%;
	}

	.skeleton-card-list {
		flex-direction: row;
		padding-left: 20px;
	}

	.skeleton-card {
		width: 200px;
		margin-right: 12px;
		border-radius: 16px;
		overflow: hidden;
		flex-shrink: 0;
	}

	.skeleton-img {
		height: 112px;
	}

	.skeleton-text {
		height: 12px;
		margin: 12px 14px 6px 14px;
	}

	.skeleton-text.short {
		width: 60%;
		margin-top: 4px;
	}

	.skeleton-list {
		padding: 0 16px;
	}

	.skeleton-list-item {
		flex-direction: row;
		align-items: center;
		padding: 12px 0;
	}

	.skeleton-thumb {
		width: 60px;
		height: 36px;
		border-radius: 6px;
		margin-right: 12px;
	}

	.skeleton-content {
		flex: 1;
	}

	.state-wrap {
		padding: 100px 40px 60px 40px;
		align-items: center;
	}

	.state-emoji {
		font-size: 56px;
		margin-bottom: 16px;
	}

	.state-text {
		font-size: 15px;
		text-align: center;
	}

	.state-sub {
		font-size: 13px;
		margin-top: 8px;
	}

	.state-btn {
		margin-top: 24px;
		padding: 12px 32px;
		border-radius: 12px;
	}

	.state-btn-text {
		font-size: 14px;
		font-weight: 600;
	}

	.data-wrap {
		padding-bottom: 10px;
	}

	.section-header {
		padding: 20px 20px 12px 20px;
	}

	.section-title-row {
		flex-direction: row;
		align-items: center;
	}

	.section-label {
		font-size: 17px;
		font-weight: 700;
	}

	.section-badge {
		font-size: 10px;
		padding: 2px 8px;
		border-radius: 4px;
		font-weight: 600;
		margin-left: 8px;
	}

	.section-badge-outline {
		font-size: 10px;
		padding: 2px 8px;
		border-radius: 4px;
		font-weight: 600;
		margin-left: 8px;
	}

	.card-scroll {
		width: 100%;
	}

	.card-list {
		flex-direction: row;
		padding-left: 20px;
		padding-right: 12px;
	}

	.game-card {
		width: 200px;
		flex-direction: column;
		margin-right: 12px;
		border-radius: 16px;
		overflow: hidden;
		flex-shrink: 0;
	}

	.game-cover-wrap {
		position: relative;
	}

	.game-cover {
		width: 200px;
		height: 112px;
	}

	.free-tag {
		position: absolute;
		top: 8px;
		left: 8px;
		font-size: 10px;
		padding: 2px 8px;
		border-radius: 4px;
		font-weight: 600;
	}

	.game-body {
		padding: 12px 14px 14px 14px;
	}

	.game-title {
		font-size: 14px;
		font-weight: 700;
		lines: 1;
	}

	.game-desc {
		font-size: 11px;
		margin-top: 4px;
		lines: 1;
	}

	.game-footer {
		margin-top: 8px;
	}

	.game-time {
		font-size: 11px;
	}

	.upcoming-list {
		padding: 0 16px;
	}

	.upcoming-item {
		flex-direction: row;
		align-items: center;
		padding: 12px 14px;
		margin-bottom: 10px;
		border-radius: 14px;
	}

	.upcoming-thumb {
		width: 56px;
		height: 36px;
		border-radius: 6px;
		margin-right: 12px;
		flex-shrink: 0;
	}

	.upcoming-content {
		flex: 1;
	}

	.upcoming-title {
		font-size: 14px;
		font-weight: 700;
		lines: 1;
	}

	.upcoming-seller {
		font-size: 11px;
		margin-top: 2px;
		lines: 1;
	}

	.upcoming-date {
		font-size: 11px;
		margin-top: 4px;
	}

	.upcoming-arrow {
		font-size: 20px;
		margin-left: 8px;
	}

	.footer-tip {
		padding: 20px;
		align-items: center;
	}

	.tip-text {
		font-size: 11px;
	}

	.bottom-safe {
		height: 34px;
	}

	/* ===================================== */
	/*          经典简约 + 浅色模式          */
	/* ===================================== */

	.home-page.theme-normal.mode-light {
		background-color: #f2f2f7;
	}

	.theme-normal.mode-light .navbar-wrap {
		background-color: #ffffff;
	}

	.theme-normal.mode-light .status-bar {
		background-color: #ffffff;
	}

	.theme-normal.mode-light .navbar {
		background-color: #ffffff;
	}

	.theme-normal.mode-light .greeting {
		color: #1c1c1e;
	}

	.theme-normal.mode-light .subtitle {
		color: #8e8e93;
	}

	.theme-normal.mode-light .icon-btn {
		background-color: #f2f2f7;
	}

	.theme-normal.mode-light .icon-btn.loading {
		background-color: #e8f2ff;
	}

	.theme-normal.mode-light .icon-text {
		color: #007aff;
	}

	.theme-normal.mode-light .skeleton-header {
		background-color: #e8e8ed;
	}

	.theme-normal.mode-light .skeleton-card {
		background-color: #ffffff;
	}

	.theme-normal.mode-light .skeleton-img {
		background-color: #e8e8ed;
	}

	.theme-normal.mode-light .skeleton-text {
		background-color: #e8e8ed;
	}

	.theme-normal.mode-light .skeleton-thumb {
		background-color: #e8e8ed;
	}

	.theme-normal.mode-light .state-text {
		color: #8e8e93;
	}

	.theme-normal.mode-light .state-sub {
		color: #c7c7cc;
	}

	.theme-normal.mode-light .state-btn {
		background-color: #007aff;
	}

	.theme-normal.mode-light .state-btn-text {
		color: #ffffff;
	}

	.theme-normal.mode-light .section-label {
		color: #1c1c1e;
	}

	.theme-normal.mode-light .section-badge {
		color: #ffffff;
		background-color: #ff3b30;
	}

	.theme-normal.mode-light .section-badge-outline {
		color: #007aff;
		background-color: #e8f2ff;
	}

	.theme-normal.mode-light .game-card {
		background-color: #ffffff;
		border: 1px solid rgba(0, 0, 0, 0.06);
	}

	.theme-normal.mode-light .game-cover {
		background-color: #e8e8ed;
	}

	.theme-normal.mode-light .free-tag {
		color: #ffffff;
		background-color: #34c759;
	}

	.theme-normal.mode-light .game-title {
		color: #1c1c1e;
	}

	.theme-normal.mode-light .game-desc {
		color: #8e8e93;
	}

	.theme-normal.mode-light .game-time {
		color: #b0b0b8;
	}

	.theme-normal.mode-light .upcoming-item {
		background-color: #ffffff;
		border: 1px solid rgba(0, 0, 0, 0.06);
	}

	.theme-normal.mode-light .upcoming-thumb {
		background-color: #e8e8ed;
	}

	.theme-normal.mode-light .upcoming-title {
		color: #1c1c1e;
	}

	.theme-normal.mode-light .upcoming-seller {
		color: #8e8e93;
	}

	.theme-normal.mode-light .upcoming-date {
		color: #b0b0b8;
	}

	.theme-normal.mode-light .upcoming-arrow {
		color: #d1d1d6;
	}

	.theme-normal.mode-light .tip-text {
		color: #c7c7cc;
	}

	/* ===================================== */
	/*          经典简约 + 深色模式          */
	/* ===================================== */

	.home-page.theme-normal.mode-dark {
		background-color: #000000;
	}

	.theme-normal.mode-dark .navbar-wrap {
		background-color: #1c1c1e;
	}

	.theme-normal.mode-dark .status-bar {
		background-color: #1c1c1e;
	}

	.theme-normal.mode-dark .navbar {
		background-color: #1c1c1e;
		border-bottom: 1px solid rgba(255, 255, 255, 0.08);
	}

	.theme-normal.mode-dark .greeting {
		color: #f5f5f7;
	}

	.theme-normal.mode-dark .subtitle {
		color: #8e8e93;
	}

	.theme-normal.mode-dark .icon-btn {
		background-color: #2c2c2e;
	}

	.theme-normal.mode-dark .icon-btn.loading {
		background-color: rgba(100, 210, 255, 0.12);
	}

	.theme-normal.mode-dark .icon-text {
		color: #64d2ff;
	}

	.theme-normal.mode-dark .skeleton-header {
		background-color: #2c2c2e;
	}

	.theme-normal.mode-dark .skeleton-card {
		background-color: #1c1c1e;
	}

	.theme-normal.mode-dark .skeleton-img {
		background-color: #2c2c2e;
	}

	.theme-normal.mode-dark .skeleton-text {
		background-color: #3a3a3c;
	}

	.theme-normal.mode-dark .skeleton-thumb {
		background-color: #2c2c2e;
	}

	.theme-normal.mode-dark .state-text {
		color: #8e8e93;
	}

	.theme-normal.mode-dark .state-sub {
		color: #636366;
	}

	.theme-normal.mode-dark .state-btn {
		background-color: rgba(100, 210, 255, 0.2);
		border: 1px solid rgba(100, 210, 255, 0.2);
	}

	.theme-normal.mode-dark .state-btn-text {
		color: rgba(180, 210, 255, 0.9);
	}

	.theme-normal.mode-dark .section-label {
		color: #f5f5f7;
	}

	.theme-normal.mode-dark .section-badge {
		color: #ffffff;
		background-color: #ff3b30;
	}

	.theme-normal.mode-dark .section-badge-outline {
		color: #64d2ff;
		background-color: rgba(100, 210, 255, 0.15);
	}

	.theme-normal.mode-dark .game-card {
		background-color: #1c1c1e;
		border: 1px solid rgba(255, 255, 255, 0.08);
	}

	.theme-normal.mode-dark .game-cover {
		background-color: #2c2c2e;
	}

	.theme-normal.mode-dark .free-tag {
		color: rgba(150, 255, 180, 0.95);
		background-color: rgba(52, 199, 89, 0.3);
	}

	.theme-normal.mode-dark .game-title {
		color: #f5f5f7;
	}

	.theme-normal.mode-dark .game-desc {
		color: #8e8e93;
	}

	.theme-normal.mode-dark .game-time {
		color: #636366;
	}

	.theme-normal.mode-dark .upcoming-item {
		background-color: #1c1c1e;
		border: 1px solid rgba(255, 255, 255, 0.08);
	}

	.theme-normal.mode-dark .upcoming-thumb {
		background-color: #2c2c2e;
	}

	.theme-normal.mode-dark .upcoming-title {
		color: #f5f5f7;
	}

	.theme-normal.mode-dark .upcoming-seller {
		color: #8e8e93;
	}

	.theme-normal.mode-dark .upcoming-date {
		color: #636366;
	}

	.theme-normal.mode-dark .upcoming-arrow {
		color: #3a3a3c;
	}

	.theme-normal.mode-dark .tip-text {
		color: #3a3a3c;
	}

	/* ===================================== */
	/*          液体玻璃主题 + 浅色模式      */
	/* ===================================== */

	.home-page.theme-glass.mode-light {
		background: linear-gradient(160deg, #f8fafc 0%, #eef2f7 25%, #e6f0ff 50%, #f0f6fb 75%, #f8fafc 100%);
	}

	.theme-glass.mode-light .navbar-wrap {
		background: rgba(255, 255, 255, 0.8);
	}

	.theme-glass.mode-light .status-bar {
		background: rgba(255, 255, 255, 0.8);
	}

	.theme-glass.mode-light .navbar {
		background: rgba(255, 255, 255, 0.8);
		border-bottom: 1px solid rgba(91, 124, 245, 0.1);
	}

	.theme-glass.mode-light .greeting {
		color: #1e293b;
	}

	.theme-glass.mode-light .subtitle {
		color: #64748b;
	}

	.theme-glass.mode-light .icon-btn {
		background: rgba(255, 255, 255, 0.6);
		border: 1px solid rgba(91, 124, 245, 0.12);
	}

	.theme-glass.mode-light .icon-btn.loading {
		background: rgba(91, 124, 245, 0.1);
	}

	.theme-glass.mode-light .icon-text {
		color: #5b7cf5;
	}

	.theme-glass.mode-light .skeleton-header {
		background: rgba(91, 124, 245, 0.1);
	}

	.theme-glass.mode-light .skeleton-card {
		background: rgba(255, 255, 255, 0.7);
		border: 1px solid rgba(91, 124, 245, 0.08);
	}

	.theme-glass.mode-light .skeleton-img {
		background: rgba(91, 124, 245, 0.1);
	}

	.theme-glass.mode-light .skeleton-text {
		background: rgba(91, 124, 245, 0.08);
	}

	.theme-glass.mode-light .skeleton-thumb {
		background: rgba(91, 124, 245, 0.1);
	}

	.theme-glass.mode-light .state-text {
		color: #64748b;
	}

	.theme-glass.mode-light .state-sub {
		color: #94a3b8;
	}

	.theme-glass.mode-light .state-btn {
		background: rgba(91, 124, 245, 0.15);
		border: 1px solid rgba(91, 124, 245, 0.2);
	}

	.theme-glass.mode-light .state-btn-text {
		color: #5b7cf5;
	}

	.theme-glass.mode-light .section-label {
		color: #1e293b;
	}

	.theme-glass.mode-light .section-badge {
		color: #ffffff;
		background-color: rgba(239, 68, 68, 0.9);
	}

	.theme-glass.mode-light .section-badge-outline {
		color: #5b7cf5;
		background-color: rgba(91, 124, 245, 0.12);
	}

	.theme-glass.mode-light .game-card {
		background: rgba(255, 255, 255, 0.7);
		border: 1px solid rgba(91, 124, 245, 0.1);
	}

	.theme-glass.mode-light .game-cover {
		background: rgba(91, 124, 245, 0.08);
	}

	.theme-glass.mode-light .free-tag {
		color: rgba(22, 163, 74, 0.95);
		background-color: rgba(34, 197, 94, 0.15);
	}

	.theme-glass.mode-light .game-title {
		color: #1e293b;
	}

	.theme-glass.mode-light .game-desc {
		color: #64748b;
	}

	.theme-glass.mode-light .game-time {
		color: #94a3b8;
	}

	.theme-glass.mode-light .upcoming-item {
		background: rgba(255, 255, 255, 0.7);
		border: 1px solid rgba(91, 124, 245, 0.1);
	}

	.theme-glass.mode-light .upcoming-thumb {
		background: rgba(91, 124, 245, 0.08);
	}

	.theme-glass.mode-light .upcoming-title {
		color: #1e293b;
	}

	.theme-glass.mode-light .upcoming-seller {
		color: #64748b;
	}

	.theme-glass.mode-light .upcoming-date {
		color: #94a3b8;
	}

	.theme-glass.mode-light .upcoming-arrow {
		color: #cbd5e1;
	}

	.theme-glass.mode-light .tip-text {
		color: #94a3b8;
	}

	/* ===================================== */
	/*          液体玻璃主题 + 深色模式      */
	/* ===================================== */

	.home-page.theme-glass.mode-dark {
		background: linear-gradient(160deg, #0a0a1a 0%, #1a1030 25%, #0d1b2a 50%, #0f0f24 75%, #0a0a1a 100%);
	}

	.theme-glass.mode-dark .navbar-wrap {
		background: rgba(255, 255, 255, 0.04);
	}

	.theme-glass.mode-dark .status-bar {
		background: rgba(255, 255, 255, 0.04);
	}

	.theme-glass.mode-dark .navbar {
		background: rgba(255, 255, 255, 0.04);
		border-bottom: 1px solid rgba(255, 255, 255, 0.08);
	}

	.theme-glass.mode-dark .greeting {
		color: rgba(255, 255, 255, 0.92);
	}

	.theme-glass.mode-dark .subtitle {
		color: rgba(190, 205, 235, 0.5);
	}

	.theme-glass.mode-dark .icon-btn {
		background: rgba(255, 255, 255, 0.06);
		border: 1px solid rgba(255, 255, 255, 0.08);
	}

	.theme-glass.mode-dark .icon-btn.loading {
		background: rgba(120, 150, 255, 0.1);
	}

	.theme-glass.mode-dark .icon-text {
		color: rgba(160, 200, 255, 0.9);
	}

	.theme-glass.mode-dark .skeleton-header {
		background: rgba(255, 255, 255, 0.06);
	}

	.theme-glass.mode-dark .skeleton-card {
		background: rgba(255, 255, 255, 0.04);
		border: 1px solid rgba(255, 255, 255, 0.06);
	}

	.theme-glass.mode-dark .skeleton-img {
		background: rgba(255, 255, 255, 0.05);
	}

	.theme-glass.mode-dark .skeleton-text {
		background: rgba(255, 255, 255, 0.05);
	}

	.theme-glass.mode-dark .skeleton-thumb {
		background: rgba(255, 255, 255, 0.05);
	}

	.theme-glass.mode-dark .state-text {
		color: rgba(200, 210, 240, 0.6);
	}

	.theme-glass.mode-dark .state-sub {
		color: rgba(160, 180, 210, 0.4);
	}

	.theme-glass.mode-dark .state-btn {
		background: rgba(120, 160, 255, 0.25);
		border: 1px solid rgba(120, 160, 255, 0.2);
	}

	.theme-glass.mode-dark .state-btn-text {
		color: rgba(180, 210, 255, 0.9);
	}

	.theme-glass.mode-dark .section-label {
		color: rgba(240, 245, 255, 0.88);
	}

	.theme-glass.mode-dark .section-badge {
		background: rgba(239, 68, 68, 0.3);
		color: rgba(255, 180, 180, 0.95);
	}

	.theme-glass.mode-dark .section-badge-outline {
		background: rgba(120, 160, 255, 0.15);
		color: rgba(140, 180, 255, 0.85);
	}

	.theme-glass.mode-dark .game-card {
		background: rgba(255, 255, 255, 0.04);
		border: 1px solid rgba(255, 255, 255, 0.07);
	}

	.theme-glass.mode-dark .game-cover {
		background-color: rgba(255, 255, 255, 0.03);
	}

	.theme-glass.mode-dark .free-tag {
		background: rgba(52, 199, 89, 0.35);
		color: rgba(150, 255, 180, 0.95);
	}

	.theme-glass.mode-dark .game-title {
		color: rgba(235, 240, 255, 0.9);
	}

	.theme-glass.mode-dark .game-desc {
		color: rgba(180, 195, 225, 0.5);
	}

	.theme-glass.mode-dark .game-time {
		color: rgba(160, 175, 210, 0.4);
	}

	.theme-glass.mode-dark .upcoming-item {
		background: rgba(255, 255, 255, 0.04);
		border: 1px solid rgba(255, 255, 255, 0.06);
	}

	.theme-glass.mode-dark .upcoming-thumb {
		background-color: rgba(255, 255, 255, 0.03);
	}

	.theme-glass.mode-dark .upcoming-title {
		color: rgba(235, 240, 255, 0.9);
	}

	.theme-glass.mode-dark .upcoming-seller {
		color: rgba(180, 195, 225, 0.5);
	}

	.theme-glass.mode-dark .upcoming-date {
		color: rgba(160, 175, 210, 0.4);
	}

	.theme-glass.mode-dark .upcoming-arrow {
		color: rgba(160, 180, 220, 0.3);
	}

	.theme-glass.mode-dark .tip-text {
		color: rgba(160, 180, 210, 0.3);
	}
</style>
